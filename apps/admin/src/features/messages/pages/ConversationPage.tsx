import { MessagesWorkspace } from '../components/MessagesWorkspace';

/** Admin conversation detail (SCR-ADM Messages, FEAT-072, ADR-013). */
export function ConversationPage() {
  return <MessagesWorkspace />;
}