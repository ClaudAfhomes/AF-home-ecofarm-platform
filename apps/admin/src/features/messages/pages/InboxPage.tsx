import { MessagesWorkspace } from '../components/MessagesWorkspace';

/** Admin Messages inbox (SCR-ADM Messages, FEAT-072, ADR-013). */
export function InboxPage() {
  return <MessagesWorkspace />;
}
